#!/usr/bin/perl

# use strict;
# use warnings;
#STDIN is known as "standard input" and allows one to wait for the user to input the data.#
#Contrast STDIN with CHOMP, STDIN also inputs carraige return.#

print "Please type in your age:";
$age = <STDIN>;

print "Your age is $age";

<STDIN>;
